name: Run

on:
  workflow_run:
    workflows: ["CI"]
    types: 
      - completed
      - requested

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Display context
        run: |
          cat << EOF
          ${{ toJSON(github) }}
          EOF
